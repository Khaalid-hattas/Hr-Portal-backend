export const getAllReports = async () => [];

export const createReport = async () => {
  throw new Error('Reports are client-side only; no reports table is available.');
};

export const deleteReportById = async () => {
  throw new Error('Reports are client-side only; no reports table is available.');
};
